import type { Metadata } from "next";
import { Inter, Lora, Montserrat, Sour_Gummy } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/theme-provider";
import Footer from "@/components/Footer";
import Subscribe from "@/components/Subscribe";

const lora = Lora({
  variable: "--font-lora",
  subsets: ["latin"],
});
const sourGummy = Sour_Gummy({
  variable: "--font-sourGummy",
  subsets: ["latin"],
});
const montserrat = Montserrat({
  variable: "--font-montserrat",
  subsets: ["latin"],
});
const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${lora.variable} ${sourGummy.variable} ${montserrat.variable} ${inter.variable} antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          <main className="px-5">{children}</main>
          <section className="my-12.5 px-5 sm:my-20">
            <Subscribe />
            <Footer />
          </section>
        </ThemeProvider>
      </body>
    </html>
  );
}
